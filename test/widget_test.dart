import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SChatApp smoke test', (WidgetTester tester) async {
    // Minimal smoke test — full integration tests require a running Matrix server.
    expect(true, isTrue);
  });
}
